import { memo } from 'react'

export const WrapperBox = memo(() => (
	<div className="wrapperBox bg-azul-950/50 fixed top-[104px] -left-full h-screen w-full transition-transform duration-300 ease-in-out will-change-transform" />
))
