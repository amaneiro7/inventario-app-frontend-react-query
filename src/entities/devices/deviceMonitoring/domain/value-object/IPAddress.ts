import { IPAddress } from '@/entities/devices/devices/domain/value-object/IPAddress'

/**
 * @class DeviceMonitoringIpAddress
 * @extends {IPAddress}
 * @description Value Object que representa la dirección IP de un dispositivo para el monitoreo.
 * Extiende del Value Object `IPAddress` general de dispositivos.
 */
export class DeviceMonitoringIpAddress extends IPAddress {}