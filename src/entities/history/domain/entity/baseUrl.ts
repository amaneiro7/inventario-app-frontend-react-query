/**
 * Base URL for history-related API endpoints.
 */
export const historyUrl = 'histories'

/**
 * URL for the history dashboard API endpoint.
 */
export const historyDashboardUrl = `${historyUrl}/dashboard`