# IMPORTANTE: Usamos una ruta relativa (sin http://dominio.com).
# Esto hace que el navegador use automáticamente el mismo dominio desde donde cargó la página.
VITE_URL_API='/api/v1/'
VITE_TITLE_LOGO='Sistema de Inventario'
